# Exact dependency versions used for every measurement reported in the paper,
# captured from the evaluation machine (Python 3.12.13, AMD Ryzen 5 5500U).
# The paper's central finding is that a single dependency version can decide
# whether a model runs at all, so we pin the environment exactly here rather
# than rely on the >= ranges in pyproject.toml.
#
# Install the full stack with:  pip install -r requirements-lock.txt
# (transformers is held at the 4.x line on purpose; 5.x breaks the
# trust_remote_code community models -- see docs/known-issues.md I5.)

# core
pyyaml==6.0.3
psutil==7.2.2
pillow==12.3.0
pandas==3.0.5
matplotlib==3.11.1
numpy==2.5.1

# models (VLMs)
torch==2.13.0
transformers==4.49.0
tokenizers==0.21.4
accelerate==1.14.0
datasets==5.0.0
einops==0.8.2
timm==1.0.28
sentencepiece==0.2.2
protobuf==7.35.1

# detectors
ultralytics==8.4.112

# quantized backends
onnxruntime==1.27.0
