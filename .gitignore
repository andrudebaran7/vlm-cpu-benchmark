.venv/
__pycache__/
*.pyc
.DS_Store
*.egg-info/

# vlmbench runtime cache (quantized exports, patched tokenizers)
.vlmbench_cache/

# Intermediate / smoke outputs (superseded by the versioned canonical results
# below; results_ci/ is the CI re-run whose per-example scores are already
# folded into results/results.jsonl).
results_ci/
results_presence_smoke/
results_ov_smoke/

# NOTE: the canonical benchmark data that backs the paper IS versioned (see
# data/README.md): results/, results_presence_coco/, results_presence_openvocab/,
# and paper_artifacts/. They are intentionally NOT ignored.

# downloaded model weights (detector checkpoints, ultralytics auto-downloads)
*.pt
weights/
